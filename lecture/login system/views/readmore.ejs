<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Readmore</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #ff9a9e, #fad0c4);
        }

        h1 {
            color: #fff;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            margin-bottom: 30px;
        }

        .card {
            border: none;
            border-radius: 25px;
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
            margin: 0 auto;
            max-width: 600px;
            overflow: hidden;
        }

        .card img {
            width: 100%;
            height: auto;
            border-top-left-radius: 25px;
            border-top-right-radius: 25px;
        }

        .blog-details {
            padding: 20px;
        }

        .blog-name {
            font-size: 1.5rem;
            font-weight: bold;
            color: #6a11cb;
            margin-bottom: 10px;
        }

        .blog-description {
            font-size: 1rem;
            color: #333;
        }

        .btn-dashboard {
            margin-top: 20px;
            display: block;
            text-align: center;
        }

        .btn-dashboard a {
            text-decoration: none;
            padding: 10px 20px;
            background-color: #6a11cb;
            color: white;
            border-radius: 25px;
            font-weight: bold;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .btn-dashboard a:hover {
            background-color: #2575fc;
        }
    </style>
</head>

<body>
    <div class="container py-5">
        <h1 class="text-center">Welcome to Readmore</h1>

        <div class="btn-dashboard">
            <a href="/dashbord">Dashboard</a>
        </div>

        <div class="card">
            <div class="p-4">
                <div class="text-center">
                    <img src="/<%= single.image %>" alt="Current Image" class="img-fluid rounded" style="max-height: 300px;">
                </div>
                <div class="blog-details">
                    <div class="blog-name"><%= single.blogename %></div>
                    <div class="blog-description"><%= single.description %></div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
