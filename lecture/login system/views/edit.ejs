<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blogs</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #ff9a9e, #fad0c4);
        }

        h1 {
            color: #fff;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            margin-bottom: 30px;
        }

        .card {
            border: none;
            border-radius: 25px;
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
            margin: 0 auto;
            max-width: 600px;
            overflow: hidden;
        }

        .card img {
            width: 100%;
            height: auto;
            border-top-left-radius: 25px;
            border-top-right-radius: 25px;
        }

        .btn-primary {
            background-color: #6a11cb;
            background-image: linear-gradient(to right, #6a11cb, #2575fc);
            border: none;
            color: #fff;
        }

        .btn-primary:hover {
            background-image: linear-gradient(to right, #2575fc, #6a11cb);
        }

        .form-control {
            border-radius: 15px;
        }

        .form-label {
            font-weight: bold;
            color: #6a11cb;
        }

        .form-group {
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
    <div class="container py-5">
        <h1 class="text-center">Welcome to Edit Blog</h1>
        <div class="card">
            <form action="/up" method="post" enctype="multipart/form-data" class="p-4">
                <input type="hidden" value="<%= single.id %>" name="id">
                
                <div class="form-group">
                    <label for="blogename" class="form-label">Blog Name:</label>
                    <input type="text" class="form-control" id="blogename" name="blogename" value="<%= single.blogename %>">
                </div>

                <div class="form-group">
                    <label for="description" class="form-label">Blog Description:</label>
                    <textarea class="form-control" id="description" name="description" rows="4"><%= single.description %></textarea>
                </div>

                <div class="form-group">
                    <label for="image" class="form-label">Image:</label>
                    <input type="file" class="form-control" id="image" name="image">
                    <div class="mt-3 text-center">
                        <img src="/<%= single.image %>" alt="Current Image" class="img-fluid rounded" style="max-height: 150px;">
                    </div>
                </div>

                <div class="text-center">
                    <button type="submit" class="btn btn-primary">Update Blog</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>