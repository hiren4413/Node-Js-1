<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #ff9a9e, #fad0c4);
        }

        h1 {
            color: #fff;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .card {
            border: none;
            border-radius: 25px;
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
            overflow: hidden;
        }

        .card img {
            width: 100%;
            height: auto;
            border-top-left-radius: 25px;
            border-top-right-radius: 25px;
        }

        .card-body {
            position: relative;
            padding-top: 15px;
        }

        .btn-primary {
            background-color: #6a11cb;
            background-image: linear-gradient(to right, #6a11cb, #2575fc);
            border: none;
            color: #fff;
        }

        .btn-primary:hover {
            background-image: linear-gradient(to right, #2575fc, #6a11cb);
        }

        .btn-danger {
            background-color: #f85032;
            background-image: linear-gradient(to right, #f85032, #e73827);
            border: none;
        }

        .btn-warning {
            background-color: #ff9966;
            background-image: linear-gradient(to right, #ff9966, #ff5e62);
            border: none;
        }
        .readdata{
            text-decoration: none;
            color: rgba(44, 44, 44, 0.6);
        }
    </style>
</head>

<body>
    <div class="container py-5">
        <div class="text-center mb-4">
            <h1>Welcome to Dashboard Page</h1>
        </div>

        <div class="text-center mb-4">
            <a href="/addbloge" class="btn btn-primary">Add Your Blogs</a>
        </div>
        

        <div class="row">
            <% user.map((val, index) => { %>
                <div class="col-md-4">
                    <div class="card">
                        <img src="<%= val.image %>" alt="Blog Image">
                        <div class="card-body">
                            <h5 class="card-title">Blog Name: <%= val.blogename %></h5>
                            <!-- <p class="card-text">Description: <%= val.description %></p> -->
                            <p><%= val.description.slice(0, 100) %>... <a href="/readmore/<%= val.id %>" class="readdata">
                                ReadMore
                            </a></p>
                            <div class="d-flex justify-content-around mt-3">
                                <a href="/deletdata/<%= val.id %>" class="btn btn-danger">Delete</a>
                                <!-- <a href="/readmore/<%= val.id %>" class="btn btn-warning">Readmore</a> -->
                                <a href="/editpage/<%= val.id %>" class="btn btn-warning">Edit</a>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    <div class="text-start mb-4"> <a href="/logeout" class="btn btn-primary">Loge Out</a></div>


    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
